// The defining of variables that will be used all throughout the code

let submit = document.getElementById("submit")
let taskpriority = document.getElementById("taskpriority")
let taskmanager = document.getElementById("taskmanager")
let p = document.createElement("p")

let tasks = {
    id: null,
    name: null,
    priority: null,
    important: null,
    date: null,
}

task.task = task.value


submit.addEventListener("click", () =>{
    console.log("Hi")
    p.innerHTML = "hi"
    taskmanager.appendChild(p)
    let new1 = document.createElement("input type=`checkbox`")
    taskmanager.appendChild(new1)

})

console.log()